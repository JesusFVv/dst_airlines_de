[sources.chess]
config_int = 0 # please set me up!

[runtime]
dlthub_telemetry = true

[destination.filesystem]
bucket_url = "file:///home/ubuntu/dst_airlines_de/lib/flights_scheduled/extraction/chess_pipeline_local_filesystem/data"
kwargs = '{"auto_mkdir": true}'
layout="{table_name}/{load_id}.{file_id}.{ext}" # current preconfigured naming scheme
# With timestamp
# layout = "{table_name}/{timestamp}/{load_id}.{file_id}.{ext}"

[normalize.data_writer]
disable_compression=true
